<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chatapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_chat_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_chat_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatApp">
        <activity android:name=".ChatSection.PersonalChatActivity"></activity>
        <activity
            android:name=".SplashActivity.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Profile.ProfileActivity"
            android:theme="@style/Theme.ChatAppLoginPage" />
        <activity
            android:name=".login.OtpActivity"
            android:theme="@style/Theme.ChatAppLoginPage" />
        <activity
            android:name=".login.LoginActivity"
            android:theme="@style/Theme.ChatAppLoginPage"></activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>